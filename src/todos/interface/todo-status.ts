
export enum TodoStatus {
    New = 'new',
    InProgress = 'wip',
    Done = 'done',
    Cancelled = 'cancelled',
    Missed = 'missed', // overdue
}

export enum TodoPriority {
    Low = 'low',
    Medium = 'medium',
    High = 'high'
}